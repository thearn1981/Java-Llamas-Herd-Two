*Creating New invoice, selecting a customer can be done by searching name or number, or if not a customer can create

*customer menu has a way to edit customers points. Maybe the customer won a drawing or used points but it did not get updated.

*Merged Mattea's csv files that contained headers and made sure the program now skips the line upon reading and adds it upon writting back to csv.

*put all the UI related parts into the UI.Java

